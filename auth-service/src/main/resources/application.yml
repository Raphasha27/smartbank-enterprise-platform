server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartbank_auth
    username: smartbank
    password: ${DB_PASSWORD:smartbank}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  application:
    name: auth-service
